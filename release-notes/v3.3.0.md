# HPE CSI Driver for Kubernetes 3.3.0 Release Notes

## HPE CSI Driver for Kubernetes 3.3.0

| **Version:** |3.3.0|
|--------------|-----|
| **Revision:** | September 2026 |


## Resolved Issues

The following table lists the resolved issues for HPE CSI Driver for Kubernetes v3.3.0.

| ID |Component |Title|Resolution |Escalation/CFI ID |
|----|----------|-----|-----------|------------------|
|CON-4863|csp.AlletraMP|ESC-17872: CSP incorrectly updates CRD to Published: false during Unpublish retry for a previously detached node|On an Unpublish retry for an already-detached node, the CSP incorrectly set the CRD to Published:false; the CRD is no longer corrupted by the retry.|CFI36173|
|CON-4909|csi.k8s|CSI issues unbounded, un-reaped NVMe/TCP discovery controllers that causes issues on the Array|The NVMe/TCP discovery-controller leak was addressed|ESC-17965|
|CON-4823|csi.k8s|ESC-17833: staging of volume fails with nvmetcp protocol err: unable to locate NVMe/TCP device|A missing NGUID on one NVMe namespace should not terminate device discovery, Log a warning and continue.|CFI35997|
|CON-4772|csi.k8s|ESC-17696 : repeat crash of hpe-csi-node pod due to OOM|Optimized Get multipath Devices functions and tuned GOMEMLIMIT for HPE CSI Node, Controller and CSP Pods|CFI35955|
|CON-4882|csp.AlletraMP|ESC-17925: Repeated OOM kill of hpe csp pod|Removed excessive logging causing OOM failures and optimzed Alletra CSP Functions|ESC-17925|
|CON-4687|csi.k8s|ESC-17375: issue with symbolic links and mounts when RWX PVC is configured and attached to POD|Symbolic-link handling on RWX/NFS mounts was fixed.|CFI35813|
|CON-4656|csp.AlletraMP|ESC-17504: CSP is enforcing the minimum size of reduced volumes as 16Gb|The CSP enforced a 16 GB minimum size for reduced (dedup/compressed) volumes; the minimum-size enforcement was fixed.|CFI35815|
|CON-4742|csi.k8s|ESC-17589: Unintended detachment/Re-enumeration of Unrelated Multipath Device During CSI PVC Detach/Attach|A PVC detach/attach could detach and re-enumerate an unrelated multipath device on the node. Device operations are now scoped to the target device so unrelated multipath devices are left untouched.|ESC-17589|
|CON-4750|csp.AlletraMP|ESC-17606: When a VM with the disk size larger than Source Image size is created, the VM Creation Fails|Provisioning a larger-than-source disk now succeeds.|CFI36183|
|CON-4768|csi.k8s|ESC-17651: NFS POD failed to start resulting in Application PODs outage|Fixed the CSI Driver to check if the Claim object still exists after we have completed the creation of the NFS Provisioner infrastructure, if not, we will trigger a cleanup to remove them.|CFI35898|
|CON-4777|csi.k8s|ESC-17752: P2 - HPE CSI crashing after update 3.0.0 to 3.1.0|After a 3.0.0->3.1.0 upgrade, hpe-csi-node was stuck in Init:CrashLoopBackOff because node init blocked on a multipathd query with a hardcoded 60s fatal timeout and always ran multipathd reconfigure. Node init no longer gates fatally on the blocking query and reconfigure is now conditional.|ESC-17752|
|CON-4879|csp.AlletraMP|ESC-17849: Orphaned snap-for-clone snapshot on array if cloned VM/pvc is deleted within 60 seconds|Deleting a cloned VM/PVC within 60 seconds left an orphaned snap-for-clone snapshot on the array; the snapshot is now cleaned up even for fast clone deletes.|CFI36181|
|CON-4887|csi.k8s|ESC-17930: HPE NFS pods entered the Terminating state when one SAN network switch was shut down|When a SAN switch was shut down, a multipath map with zero path groups was force-removed, leaving HPE-NFS pods stuck in Terminating. Device teardown now gates on multipath health and uses `multipath -f` instead of a forced `dmsetup remove`.|ESC-17930|
|CON-4193|csp.AlletraMP|VM stop and subsequent start does not work in case of primary storage array failure|In Active Peer Persistence, publish now succeeds when at least one array (the recovery/secondary site) is reachable, so a VM can start while the primary array is down.| |
|CON-4384|csp.AlletraMP|Cloning Async PVC into "active-active" RCG with Async replication CRD throws no error|Cloning an Async PVC into an active-active RCG using an Async replication CRD silently "succeeded" without adding the volume to any RCG. The invalid clone/replication-policy combination is now validated and rejected.| |
|CON-4387|csi.k8s|NVMe connectivity/paths are not re-established after array reboot|NVMe/TCP connections were not persistent across an array reboot (empty `nvme list`, I/O errors). With --ctrl-loss-tmo seto to -1, kernel re-establishes NVMe/TCP paths infinitely| |
|CON-4399|csp.AlletraMP|200 PVC/Pod creation shows multiple CSP errors, panics and PVC takes 10+ min to all bind|Concurrent 200-PVC provisioning produced nil-pointer panics and long bind times. Panic/error handling and array-session handling under load were hardened.| |
|CON-4426|csp.AlletraMP|CSI3.1-VVSet not created on the secondary array after created the volumegroup in k8s cluster|VVSet (volume group) create operations are now synced to the secondary array for replicated volumes.| |
|CON-4430|csp.AlletraMP|CSI3.1-Openshift-VVSet not removed after deleting the volumegroup in array side|VVSet delete operations are now synced to the secondary array so the group is removed on both arrays.| |
|CON-4534|csp.AlletraMP|Sync snapshot group VVset operations to secondary array for replicated volumes|Snapshot-group / VVSet operations are now synced to the secondary array, and unsupported volumegroup/snapshotgroup operations on replicated PVCs are restricted with an appropriate error.| |
|CON-4645|csi.k8s|fsRepair param does not validate the input and no error is reported, silently applies any values input|The `fsRepair` StorageClass parameter now validates its input and only accepts `true`/`false`.| |
|CON-4668|csp.AlletraMP|unable to clone PVC to higher size using cloneOf parameter|Cloning a PVC to a larger size than the parent using `cloneOf` now succeeds (previously failed with "size is not equal to requested size").| |
|CON-4680|csp.AlletraMP|CSI 3.2.0 creates APP PVC when "replicaitonPolicy" field is set to "" but PV's yaml's replicationPolicy Field is also null|A null `replicationPolicy` now correctly creates an APP PVC and the PV's attributes reflect the correct replication policy.| |
|CON-4699|csp.AlletraMP|SnapshotGroup with maxlength (253 chars) creates, but volumesnapshot is not created|A max-length (253-char) SnapshotGroup was accepted but produced no VolumeSnapshots. Name-length handling/validation fixed.| |
|CON-4778|csp.AlletraMP|Matched set with AAPP in RWX Block pod does not have expected number of active vLUNs on Primary|For matched-set (hostSeesVLUN=false) AAPP RWX Block volumes, all expected VLUN paths are now exported on the primary array.| |
|CON-4785|csp.AlletraMP|Node failover/publish with AAPP Pods during primary array down does not succeed|Node failover/new publish for AAPP pods now succeeds while the primary array is down.| |
|CON-4802|csp.AlletraMP|Failing to remove vLUNs from secondary array for AAPP hostseesvlun Block RWX volumes during scale down|On scale-down of AAPP hostSeesVLUN RWX Block deployments, VLUNs are now removed from the secondary array as well.| |
|CON-4804|csp.AlletraMP|WSAPI returns HTTP 409 - resource in use when a session deletes itself on logout (Alletra MP B10130 / arcus)|WSAPI self-session logout returned HTTP 409. Session/logout handling adjusted so teardown no longer 409s (array/WSAPI interaction).| |
|CON-4852|csp.AlletraMP|VolumeAttachment deleted while ControllerUnpublishVolume but leaving stale export/ACL entry on array, and blocking delete|ControllerUnpublishVolume no longer leaves a stale export/ACL entry on the array that blocked subsequent volume delete.| |
|CON-4883|csp.AlletraMP|For Clone and Restore Volume virtualDomain field gets Omitted in CRD|Clone and Restore volumes now persist the `VirtualDomain` field in the HPEVolumeInfo CRD, matching the source volume.| |
|CON-4885|csp.AlletraMP|ControllerPublishVolume silently succeeds with LunId=-1 when array host-lookup returns 404, blocking all future publish/attach|A concurrently-deleted array host caused the CSP to write Published=true / LunId=-1 and then short-circuit all retries. The publish path no longer persists a failed publish and remains retryable.| |
|CON-4897|csp.AlletraMP|During primary array down, VM failover to new worker node does not succeed|VM failover to a new worker node while the primary array is down now succeeds.| |
|CON-4925|csp.AlletraMP|Async replicated PV and RCGs are not cleared upon PVC deletion as CSI's DeleteVolume mistakes Remote Copy's internal rcpy.* snapshot for a user snapshot and skips delete|DeleteVolume counted Remote Copy's internal `rcpy.*` vcopy snapshots as user snapshots and refused delete, orphaning volumes/RCGs. These internal snapshots are now excluded so async-replicated volumes delete cleanly.| |
|CON-4911|csp.AlletraMP|AAPP: ControllerPublishVolume returns primary LunId=-1 during primary-array failover; CSP publishes recovery array as a peer, driver rejects attach ("invalid LUN ID -1")|During AAPP primary-array failover the publish now returns a valid LUN from the recovery array instead of LunId=-1.| |
|CON-4881|csi.k8s|CHL ExecCommand merges stdout/stderr even if exit code is 0|common-host-libs `ExecCommand` no longer merges stderr into stdout on success (exit code 0); stdout is returned alone.| |
|CON-4177|csp.b10kfile|Static provisioning with Unified File is failing when attaching PVC to Pod|Static (pre-provisioned) Unified File shares could not be looked up by the CSP on attach ("get file share error" / 403). Static provisioning fileshare resolution fixed.| |
|CON-4857|csp.b10kfile|Removing the space limit after failed provisioning on Unified File causes "filesystem already exists" failure|Retrying Unified File provisioning after removing a space limit no longer fails with "filesystem already exists" (idempotent handling of the partially-created filesystem).| |

## Known Issues

Please note that, `Known Issues` from previous releases are still applicable with suggested workarounds, if they are not part of `Resolved Issues` above.

| ID | Component | Title | Description | Workaround |
|----|-----------|-------|-------------|------------|
|CON-4438|csp.3par|Ephemeral inline pods using more than one volume, fail when pod names are longer|Long ephemeral Pod names cause pvc names to be truncated and cause issues, when ephemeral pod has more than 1 volume|Issue is not seen if Ephemeral Pod Names are limited to 11 characters|
|CON-4181|csp.AlletraMP|Annotating AAPP volume to a new RCG shows error on PVC describe|Moving an AAPP volume to a new active-active RCG via annotations updates the array/CRD correctly but `kubectl describe pvc` still reports an error.| VMs remain running/accessible; error on describe can be ignored |
|CON-4218|csp.AlletraMP|CPP/APP - Snapshot is not deleted at secondary array|For CPP/APP replicated volumes, deleting a snapshot removes it from the primary array only; the secondary-array snapshot remains.| Manually remove the snapshot on the secondary array |
|CON-4281|csp.AlletraMP|Overriding async PVC's periodicReplicationInterval for existing RCG takes long time to edit|Changing `periodicReplicationInterval` on a PVC in an existing async RCG takes several minutes to apply the new sync period on the array.| Wait for the edit to converge (several minutes) |
|CON-4293|csp.AlletraMP|virtualCopyof parameter searches in every namespace for original PVC|`virtualCopyOf` searches for the source PVC across all namespaces; a clone created in a different namespace than the source binds but the pod fails to start.| Create the clone PVC in the same namespace as the source PVC |
|CON-4367|csi.k8s|nvme list-subsys output is not empty sometimes after deleting all pods|After deleting all pods on a node, `nvme list-subsys` occasionally still shows `connecting` subsystems (mounts/VLUNs/PVCs already removed); not consistently reproducible.| Clears after ~20-30 min|
|CON-3873|csi.k8s|fsMode and fsOwner should only fire during first staging|`fsMode`/`fsOwner` recursively `chmod`/`chown` the filesystem on every publish, hurting performance on large filesystems; they should apply only at first staging.| None |
|CON-4379|csi.k8s|CSI3.1 NVMeTCP: Worker node reboot causes connectivity loss; pods fail to migrate|After a worker-node reboot on NVMe/TCP, connectivity is lost and pods fail to migrate.| None |
|CON-4468|csi.k8s|iscsiPortalIps parameter is not working as intended|The `iscsiPortalIps` StorageClass parameter does not restrict/select iSCSI portals as intended.| None |
|CON-4543|csp.AlletraMP|Ephemeral volumes with AAPP fails but works with Classic/Async RCG|Ephemeral inline pods with active-active (AAPP) replication get stuck in ContainerCreating ("no hosts found for the secondary volume to export to"), while Classic/Async RCG works.| Use Classic/Async replication for ephemeral inline volumes |
|CON-4628|csi.k8s|CSI Driver prefers helm chart CHAP values over SC yaml values|When CHAP is supplied both via the Helm chart (cluster-wide) and the StorageClass, the driver uses the Helm chart CHAP values.| Configure CHAP in only one place |
|CON-4629|csi.k8s|disablePreInstallHooks set to true is still using CHAP secret given in Helm Chart YAML|With `disablePreInstallHooks: true` and a Helm CHAP secret set, the host is still configured with CHAP.| Do not supply the Helm CHAP secret if hooks are disabled |
|CON-4655|csp.AlletraMP|PV deleted when RCG is in stopped state|Deleting a replicated PVC while its RCG is stopped on the secondary array deletes the PV instead of leaving it in a marked-for-delete state.| Ensure the RCG is started before deleting replicated PVCs |
|CON-4688|csp.AlletraMP|Description of newly created replicated volumes is not set on creation|On creation, the StorageClass `description` is set on the primary-array VV but not on the secondary-array VV.| Mutate the description after creation (CON-4290) |
|CON-4690|csp.AlletraMP|Snapshotting a replicated statically provisioned PVC does not deleted on secondary array|A snapshot of a replicated, statically-provisioned PVC is not deleted on the secondary array.| Manually remove the snapshot on the secondary array |
|CON-4795|csp.b10kfile|Static provisioning with Unified File is failing when persistentVolumeReclaimPolicy set as Delete|A statically-provisioned Unified File PV with `reclaimPolicy: Delete` stays `Released` after PVC deletion (DeleteVolume fails).| Use `reclaimPolicy: Retain` for static Unified File PVs |
|CON-4815|csi.k8s|Node Pod stuck Terminating because a multipath device with zero paths hangs I/O forever (queue_if_no_path), leaving an unkillable kernel thread|When all paths of a multipath device disappear (LUN removed while mounted), `queue_if_no_path` queues I/O forever; an ordinary driver directory-listing thread blocks in uninterruptible (D) sleep, so the pod can never finish terminating.| Reboot the node to clear the stuck kernel thread |
|CON-4901|csi.k8s|NodeUnstageVolume orphans a live LUKS mapping and mount when staging was interrupted|If NodeStageVolume is interrupted after creating the LUKS mapping/mount but before writing deviceInfo.json, NodeUnstageVolume assumes it was never staged and skips teardown, leaving an active dm-crypt mapping and ext4 mount on a deleted device.| Manually remove the orphaned dm-crypt mapping/mount on the node |
|CON-4907|csi.k8s|Concurrent live migration leaves orphaned device-mapper multipath maps and hundreds of failed paths on the source node, requiring manual cleanup|After concurrent live migration, the source node retains orphaned multipath maps (zero active paths) and hundreds of failed paths that do not self-clear.| Run `multipath -f` per orphan map, or reboot the node |
|CON-4910|csp.AlletraMP|For Virtual Copy of Volume virtualDomain field gets Omitted in CRD|A virtualCopyOf (virtual copy) volume's HPEVolumeInfo CRD omits the `VirtualDomain` field carried by the source volume (related to CON-4883).| None |
|CON-4930|csp.AlletraMP|deleteSnapshotOnSecondaryArray resolves the parent volume CRD with a truncated name, so peer-array snapshot cleanup never executes|On clone/restore volume delete, `deleteSnapshotOnSecondaryArray` looks up the parent CRD using the 31-char truncated backend name against the full 40-char CSI volume ID, so it always misses; peer-array snapshot cleanup never runs and an ERROR line is emitted (related to CON-4879).| Manually remove the stranded base snapshot on the secondary array |
|CON-4934|csi.k8s|Controller-node reboot: multipath maps never re-add their paths to the returning controller, silently leaving volumes pinned to a single controller|After an array controller node reboots and rejoins, iSCSI sessions reconnect but the per-LUN SCSI devices are not rescanned back into the device-mapper maps, leaving volumes running at half redundancy while all health checks report healthy.| Rescan/reload multipath maps (or reboot the worker) after controller failback |
|CON-4935|csi.k8s|Cold-target volume staging during an array controller outage exceeds the KubeVirt migration timeout, failing 46 of 60 concurrent live migrations|When live migration targets a node that has not previously staged the volumes and an array controller is simultaneously down, cold NodeStageVolume for 10 disks/VM cannot finish inside KubeVirt's 900 s window (46/60 fail vs 1/60 warm).| Pre-warm target nodes; avoid draining during a controller outage |
|CON-4944|csp.AlletraMP|CLONE - HPEReplicationMappings does not revert once original primary is back up|During AAPP primary-down testing, `HPEReplicationMapping` flips primary/target to the recovery array and does not revert when the original primary returns.|Delete the HPEReplicationMappings CRD when the Primary is back up, CSP will recreate the CRD.|
|CON-4946|csp.AlletraMP|Failover of AAPP VM/pod when primary or secondary array is down takes ~20 min|AAPP VM/pod failover with the primary (or secondary) array down takes ~19-26 minutes to publish to the new node and reach Running (the CSP loops on session/health retries to the dead array before serving from the recovery site).| Allow up to ~20-26 min for failover to complete |
|CON-4787|csp.AlletraMP|Node failover/publish with AAPP Pods during secondary array down does not succeed|When an Active-Active Peer Persistence (AAPP) RWX Block pod is failed over or newly published to another worker node while the secondary array is down, ControllerPublishVolume does not succeed and the pod stays in ContainerCreating (companion to CON-4193, which covers the primary-array-down case). While the peer is down the host is not admitted to the Active-Active group on the reachable array (array error code 29), and there is no degraded single-array AAPP publish path.| None |
